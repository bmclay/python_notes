# Get user input using the input function
# Specify a string to be displayed at the prompt. Add a space to make it look better for the user.
input('Enter name and press enter: ')
# Store the value by assigning the input to a variable, so you can use it later.
name = input('Name: ')
print(name)
# You can take multiple user inputs
age  = input('Age: ')
print('Hello', name, 'you are', age, 'years old')